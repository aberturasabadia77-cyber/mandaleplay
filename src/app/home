export default function Home() {
  return (
    <main style={{ minHeight: '100vh', background: '#060612', color: '#fff', fontFamily: 'system-ui, -apple-system, sans-serif', overflowX: 'hidden' }}>

      <style>{`
        @keyframes pulse { 0%, 100% { opacity: 1 } 50% { opacity: 0.6 } }
        @keyframes float { 0%, 100% { transform: translateY(0px) } 50% { transform: translateY(-8px) } }
        .neon-btn {
          background: linear-gradient(135deg, #00f5ff, #8b5cf6);
          color: #000;
          border: none;
          padding: 16px 40px;
          border-radius: 100px;
          font-size: 17px;
          font-weight: 700;
          cursor: pointer;
          text-decoration: none;
          display: inline-block;
          box-shadow: 0 0 30px rgba(0,245,255,0.4), 0 0 60px rgba(139,92,246,0.2);
          transition: box-shadow .3s, transform .2s;
        }
        .neon-btn:hover {
          box-shadow: 0 0 50px rgba(0,245,255,0.6), 0 0 80px rgba(139,92,246,0.4);
          transform: translateY(-2px);
        }
        .card {
          background: rgba(255,255,255,0.03);
          border: 1px solid rgba(0,245,255,0.15);
          border-radius: 20px;
          padding: 28px;
          transition: border-color .3s, box-shadow .3s;
        }
        .card:hover {
          border-color: rgba(0,245,255,0.4);
          box-shadow: 0 0 20px rgba(0,245,255,0.1);
        }
      `}</style>

      {/* Fondo con gradientes */}
      <div style={{ position: 'fixed', inset: 0, zIndex: 0, pointerEvents: 'none', overflow: 'hidden' }}>
        <div style={{ position: 'absolute', top: '-20%', left: '-10%', width: '600px', height: '600px', background: 'radial-gradient(circle, rgba(0,245,255,0.08) 0%, transparent 70%)', borderRadius: '50%' }} />
        <div style={{ position: 'absolute', top: '30%', right: '-15%', width: '500px', height: '500px', background: 'radial-gradient(circle, rgba(139,92,246,0.1) 0%, transparent 70%)', borderRadius: '50%' }} />
        <div style={{ position: 'absolute', bottom: '10%', left: '20%', width: '400px', height: '400px', background: 'radial-gradient(circle, rgba(0,245,255,0.06) 0%, transparent 70%)', borderRadius: '50%' }} />
      </div>

      {/* Nav */}
      <nav style={{ position: 'relative', zIndex: 10, display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '20px 40px', borderBottom: '1px solid rgba(0,245,255,0.1)' }}>
        <div style={{ fontSize: '22px', fontWeight: '700', letterSpacing: '-0.5px' }}>
          Mandale{' '}
          <span style={{ background: 'linear-gradient(135deg, #00f5ff, #8b5cf6)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent' }}>
            Play
          </span>
        </div>
        <a href="/evento" className="neon-btn" style={{ padding: '10px 24px', fontSize: '14px' }}>
          Crear mi plan
        </a>
      </nav>

      {/* Hero */}
      <section style={{ position: 'relative', zIndex: 10, maxWidth: '820px', margin: '0 auto', padding: '90px 24px 60px', textAlign: 'center' }}>

        <div style={{ display: 'inline-block', background: 'rgba(0,245,255,0.08)', border: '1px solid rgba(0,245,255,0.3)', borderRadius: '100px', padding: '6px 20px', fontSize: '12px', letterSpacing: '2px', textTransform: 'uppercase', marginBottom: '32px', color: '#00f5ff' }}>
          DJ inteligente para tu evento
        </div>

        <h1 style={{ fontSize: 'clamp(38px, 7vw, 76px)', fontWeight: '800', lineHeight: 1.05, marginBottom: '24px', letterSpacing: '-2px' }}>
          La música perfecta<br />
          para{' '}
          <span style={{ background: 'linear-gradient(135deg, #00f5ff, #8b5cf6)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent' }}>
            tu fiesta
          </span>
        </h1>

        <p style={{ fontSize: 'clamp(16px, 2vw, 20px)', color: '#888', lineHeight: 1.7, maxWidth: '560px', margin: '0 auto 48px' }}>
          Contanos qué festejás, cuánta gente viene y qué energía querés. La IA arma el timeline musical completo como lo haría un DJ profesional.
        </p>

        <a href="/evento" className="neon-btn">
          ✨ Crear mi plan musical gratis
        </a>
        <div style={{ fontSize: '13px', color: '#444', marginTop: '16px' }}>Sin registrarse · Listo en 30 segundos</div>
      </section>

      {/* Timeline demo */}
      <section style={{ position: 'relative', zIndex: 10, maxWidth: '720px', margin: '0 auto', padding: '0 24px 80px' }}>
        <div className="card" style={{ padding: '32px' }}>
          <div style={{ fontSize: '11px', color: '#00f5ff', letterSpacing: '2px', textTransform: 'uppercase', marginBottom: '20px' }}>
            Ejemplo — Quincho, 3 horas, energía animada
          </div>

          {/* Barra */}
          <div style={{ display: 'flex', borderRadius: '12px', overflow: 'hidden', height: '48px', marginBottom: '10px', gap: '2px' }}>
            {[
              { label: 'Llegada', color: '#00f5ff', w: 18 },
              { label: 'Aperitivo', color: '#22d3ee', w: 18 },
              { label: 'Comida', color: '#8b5cf6', w: 28 },
              { label: 'Pico', color: '#a855f7', w: 22 },
              { label: 'Cierre', color: '#6366f1', w: 14 },
            ].map((b, i) => (
              <div key={i} style={{ width: `${b.w}%`, background: b.color, opacity: 0.8, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <span style={{ fontSize: '9px', fontWeight: '700', color: '#000' }}>{b.label}</span>
              </div>
            ))}
          </div>

          <div style={{ display: 'flex', gap: '2px', marginBottom: '24px' }}>
            {[
              { dur: '20 min', bpm: '75-85 BPM', color: '#00f5ff', w: 18 },
              { dur: '20 min', bpm: '85-95 BPM', color: '#22d3ee', w: 18 },
              { dur: '35 min', bpm: '95-115 BPM', color: '#8b5cf6', w: 28 },
              { dur: '25 min', bpm: '115-130 BPM', color: '#a855f7', w: 22 },
              { dur: '20 min', bpm: '100-115 BPM', color: '#6366f1', w: 14 },
            ].map((b, i) => (
              <div key={i} style={{ width: `${b.w}%` }}>
                <div style={{ fontSize: '9px', color: b.color, fontWeight: '600' }}>{b.dur}</div>
                <div style={{ fontSize: '8px', color: '#444' }}>{b.bpm}</div>
              </div>
            ))}
          </div>

          {[
            { titulo: 'De Música Ligera', artista: 'Soda Stereo', bpm: 78, color: '#00f5ff' },
            { titulo: 'La Balsa', artista: 'Los Gatos', bpm: 85, color: '#22d3ee' },
            { titulo: 'Maradona', artista: 'Fito Páez', bpm: 102, color: '#8b5cf6' },
            { titulo: 'La Bamba', artista: 'Los Lobos', bpm: 124, color: '#a855f7' },
          ].map((c, i) => (
            <div key={i} style={{ display: 'flex', alignItems: 'center', gap: '10px', padding: '8px 0', borderBottom: '1px solid rgba(255,255,255,0.05)' }}>
              <span style={{ color: c.color, fontSize: '10px' }}>♪</span>
              <span style={{ fontSize: '13px', color: '#888', flex: 1 }}>{c.titulo} — {c.artista}</span>
              <span style={{ fontSize: '11px', color: c.color, fontWeight: '700' }}>{c.bpm} BPM</span>
            </div>
          ))}
        </div>
      </section>

      {/* Cómo funciona */}
      <section style={{ position: 'relative', zIndex: 10, maxWidth: '820px', margin: '0 auto', padding: '0 24px 80px' }}>
        <h2 style={{ fontSize: 'clamp(24px, 4vw, 40px)', fontWeight: '700', textAlign: 'center', marginBottom: '12px' }}>
          Cómo funciona
        </h2>
        <p style={{ color: '#555', textAlign: 'center', marginBottom: '48px', fontSize: '15px' }}>Tres pasos y tu noche tiene música perfecta</p>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '20px' }}>
          {[
            { num: '01', color: '#00f5ff', titulo: 'Contanos tu evento', desc: 'Tipo de reunión, personas, energía, horario. Como le explicarías a un DJ.' },
            { num: '02', color: '#8b5cf6', titulo: 'La IA arma el timeline', desc: 'Bloques de música con BPMs verificados y progresión de energía profesional.' },
            { num: '03', color: '#a855f7', titulo: 'Dale play', desc: 'Reproducción automática, sin silencios, con enganches entre canciones.' },
          ].map((p, i) => (
            <div key={i} className="card">
              <div style={{ fontSize: '32px', fontWeight: '800', background: `linear-gradient(135deg, ${p.color}, #8b5cf6)`, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', marginBottom: '14px' }}>{p.num}</div>
              <div style={{ fontSize: '16px', fontWeight: '600', marginBottom: '8px' }}>{p.titulo}</div>
              <div style={{ fontSize: '13px', color: '#666', lineHeight: 1.7 }}>{p.desc}</div>
            </div>
          ))}
        </div>
      </section>

      {/* Diferenciadores */}
      <section style={{ position: 'relative', zIndex: 10, maxWidth: '820px', margin: '0 auto', padding: '0 24px 80px' }}>
        <h2 style={{ fontSize: 'clamp(24px, 4vw, 40px)', fontWeight: '700', textAlign: 'center', marginBottom: '12px' }}>
          No es una playlist.<br />
          Es un{' '}
          <span style={{ background: 'linear-gradient(135deg, #00f5ff, #8b5cf6)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent' }}>
            plan.
          </span>
        </h2>
        <p style={{ color: '#555', textAlign: 'center', marginBottom: '48px', fontSize: '15px' }}>Lo que ninguna app de música hace</p>

        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '16px' }}>
          {[
            { emoji: '🎯', color: '#00f5ff', titulo: 'Contexto real', desc: 'No es "poneme música tranquila". Es "asado, 20 personas, mixto de edades, arranca a las 13".' },
            { emoji: '🎚️', color: '#8b5cf6', titulo: 'BPMs verificados', desc: 'Cada canción tiene el BPM real. Nunca más un tema lento en medio de la pista.' },
            { emoji: '🌙', color: '#a855f7', titulo: 'Arco de energía', desc: 'El plan sube y baja la energía exactamente cuando tiene que hacerlo.' },
            { emoji: '🇦🇷', color: '#6366f1', titulo: 'Pensado para Argentina', desc: 'Soda Stereo, cumbia, Gardel. Entiende la cultura local.' },
          ].map((d, i) => (
            <div key={i} className="card">
              <div style={{ fontSize: '28px', marginBottom: '12px' }}>{d.emoji}</div>
              <div style={{ fontSize: '15px', fontWeight: '600', marginBottom: '8px', color: d.color }}>{d.titulo}</div>
              <div style={{ fontSize: '13px', color: '#555', lineHeight: 1.7 }}>{d.desc}</div>
            </div>
          ))}
        </div>
      </section>

      {/* CTA */}
      <section style={{ position: 'relative', zIndex: 10, textAlign: 'center', padding: '60px 24px 80px', borderTop: '1px solid rgba(0,245,255,0.1)' }}>
        <div style={{ fontSize: '48px', marginBottom: '20px', animation: 'float 3s ease-in-out infinite' }}>🎵</div>
        <h2 style={{ fontSize: 'clamp(28px, 5vw, 52px)', fontWeight: '800', marginBottom: '16px', letterSpacing: '-1px' }}>
          ¿Tenés un evento?{' '}
          <span style={{ background: 'linear-gradient(135deg, #00f5ff, #8b5cf6)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent' }}>
            Mandále Play.
          </span>
        </h2>
        <p style={{ color: '#555', fontSize: '16px', marginBottom: '40px' }}>Gratis. Sin registro. Listo en 30 segundos.</p>
        <a href="/evento" className="neon-btn">✨ Crear mi plan musical</a>
      </section>

      {/* Footer */}
      <footer style={{ position: 'relative', zIndex: 10, borderTop: '1px solid rgba(0,245,255,0.08)', padding: '24px 40px', display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '12px' }}>
        <div style={{ fontSize: '16px', fontWeight: '700' }}>
          Mandale{' '}
          <span style={{ background: 'linear-gradient(135deg, #00f5ff, #8b5cf6)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent' }}>
            Play
          </span>
        </div>
        <div style={{ fontSize: '12px', color: '#333', display: 'flex', gap: '16px', flexWrap: 'wrap', alignItems: 'center' }}>
          <span>© 2026 · Hecho con IA en Argentina 🇦🇷</span>
          <a href="/terminos" style={{ color: '#444', textDecoration: 'none' }}>Términos</a>
          <a href="/privacidad" style={{ color: '#444', textDecoration: 'none' }}>Privacidad</a>
          <span>BPM data by <a href="https://getsongbpm.com" target="_blank" rel="noopener noreferrer" style={{ color: '#444', textDecoration: 'underline' }}>GetSongBPM</a></span>
        </div>
      </footer>

    </main>
  )
}
